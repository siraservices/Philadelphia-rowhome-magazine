Drop advertiser tile images here (JPG/PNG, portrait ~600x800). They appear in
the homepage "PRH Magazine Ad's Directory" strip in filename order.
Optional: name a file `01-tonylukes.jpg` and add its link in
`rowhome_advertiser_tiles()` (inc/homepage-helpers.php) via the $links map.
